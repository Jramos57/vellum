/// # vellum
///
/// Strict EPUB processing library for creating, parsing, and extracting text from EPUB books.
public enum vellum {
    /// Library version.
    public static let version = "0.1.0"
}
